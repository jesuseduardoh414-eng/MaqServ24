import { PaginaCatalogoAdmin } from '../../productos/catalogo-page';

/** Productos del catálogo: lo que se vende a precio fijo (hoy, ninguno). */
export default function AdminProductos() {
  return <PaginaCatalogoAdmin tipo="producto" />;
}
